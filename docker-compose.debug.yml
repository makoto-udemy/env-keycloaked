services:
  keycloak:
    ports:
      - "${KEYCLOAK_HOST_PORT:-18080}:8080"

  frontend:
    ports:
      - "${FRONTEND_HOST_PORT:-15173}:5173"

  backend:
    ports:
      - "${BACKEND_HOST_PORT:-18000}:8000"
